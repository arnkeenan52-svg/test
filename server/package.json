{
  "name": "roblox-ai-dev-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local bridge server for the Roblox AI Dev Tool (Slice 1). Holds the Anthropic API key and turns natural-language requests into structured instance specs.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.105.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
